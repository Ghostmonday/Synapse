/**
 * Root bootstrap file (used for local dev build demonstration)
 * In final build, server code is in src/server/index.ts and compiled to dist/.
 *
 * This file exists so the flat packaging can be run directly during dev if desired.
 */

import './src/server/index.js';

